# Server-side Gemini API key (NEVER prefix with VITE_ — must stay server-only).
# Get one at https://aistudio.google.com/
GEMINI_API_KEY=

# Optional: force the mock extraction pipeline (skip Gemini entirely).
# Set to "true" only for offline development. Default is unset (real pipeline).
VITE_USE_MOCK=
